{
  "name": "adjskit",
  "version": "0.0.0",
  "description": "CLI to scaffold and manage discord.js bot projects built on @adjskit/core.",
  "type": "module",
  "bin": {
    "adjskit": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20.12.0"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@adjskit/core": "workspace:*",
    "@clack/prompts": "^1.7.0",
    "commander": "^15.0.0",
    "picocolors": "^1.1.1",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.3.5",
    "vitest": "^3.0.0"
  }
}
